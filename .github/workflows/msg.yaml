name: GitHub Actions
on:
  push:
      branches:
            - main
jobs:
    message:
      runs-on: ubuntu-latest
      steps:
        - name: message
          run: echo "This is Github Actions"
        - name: list env
          run: printenv